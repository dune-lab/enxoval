{
  "name": "enxoval",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "types",
    "db",
    "http",
    "messaging",
    "observability",
    "docker",
    "testing",
    "quality",
    "auth"
  ],
  "scripts": {
    "build": "npm run build -w types -w observability -w db -w http -w messaging -w testing -w quality -w auth",
    "publish:all": "npm run build && npm publish --workspaces --access public"
  }
}
