{
  "name": "@enxoval/auth",
  "version": "1.0.0",
  "description": "JWT auth middleware for Fastify services: setupAuth and getCurrentUser",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "auth",
    "jwt",
    "fastify",
    "typescript"
  ],
  "license": "MIT",
  "dependencies": {
    "@enxoval/http": "file:../http",
    "@enxoval/types": "file:../types",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.9",
    "typescript": "^5.7.2"
  }
}
